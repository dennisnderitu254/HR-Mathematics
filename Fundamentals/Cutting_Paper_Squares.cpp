#include <bits/stdc++.h>

using namespace std;

#define ll long long
// m-1+m(n-1)=mn-1

int main() {
    ios::sync_with_stdio(false);
    ll n, m;
    cin >> n >> m;
    cout << m * n - 1 << endl;
    return 0;
}